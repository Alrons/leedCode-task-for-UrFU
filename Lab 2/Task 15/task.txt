Объяснение:

n = 3 поскольку имеется 3 числа, то все числа попадают в диапазон [0,3]. Число 2 является пропущенным числом в диапазоне, так как оно не встречается в nums.

Пример 2:

Ввод: числа = [0,1]

Выход: 2

Объяснение:

n = 2 поскольку имеется 2 числа, то все числа попадают в диапазон [0,2]. 2 — пропущенное число в диапазоне, так как оно не встречается в nums.

Пример 3:

Ввод: числа = [9,6,4,2,3,5,7,0,1]

Выход: 8

Объяснение:

n = 9 поскольку имеется 9 чисел, то все числа попадают в диапазон [0,9]. Число 8 является пропущенным числом в диапазоне, так как оно не встречается в nums.